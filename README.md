# Abstract Clock

An abstract clock visualization using p5.js. Three balls (large = hour, medium = minute, small = second) move along a track to display time.

## Usage

Open `index.html` in your browser, or run a local server:

```bash
python -m http.server 8000
```

## Tech Stack

- [p5.js](https://p5js.org/)
